# skyglow-anomaly

Project for detecting long-term changes in local sky brightness across a given grid tile of geospatial satellite imagery. Requires `pandas`, `numpy`, `matplotlib` and `rasterio`. Details and use cases coming soon. 
